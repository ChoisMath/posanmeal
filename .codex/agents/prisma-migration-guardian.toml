developer_instructions = ".codex/rules/nextjs-prisma.md와 AGENTS.md를 읽는다. 요청된 schema/SQL diff에서 기본값 없는 NOT NULL, drop/rename, unique 중복, 타입/enum 변경, Cascade, 잠금과 이전 앱 호환성을 검토한다. 이 프로젝트 unique는 실제 생성 SQL에서 INDEX/CONSTRAINT 형태를 확인한다. additive 및 단계별 배포를 우선한다. DB 명령은 실행하지 않고 차단/경고/권고와 근거를 보고한다."
